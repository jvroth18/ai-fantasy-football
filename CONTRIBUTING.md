# Contributing

Use a focused branch and keep commits small. Each behavior change must include tests and pass the relevant checks before it is committed.

```bash
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

Do not include private league data, screenshots, credentials, downloaded provider datasets, or generated local databases in issues or pull requests.
